.DS_Store
node_modules/
dist/
.env
.env.*
!.env.example
.claude/
# The tvOS project under nativescript/ is its own pnpm root (its own lockfile is tracked);
# its build output and installed hooks are not.
nativescript/node_modules/
nativescript/platforms/
nativescript/hooks/
